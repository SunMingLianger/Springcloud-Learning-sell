eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          username: SunMingLianger
          password: s321000
          uri: https://github.com/SunMingLianger/config-repo
